# Minimal config so `chat-push` can be invoked by a Database Webhook
# (the function checks x-webhook-secret itself).

project_id = "hqaxwodbhaohwyunbzxg"

[functions.chat-push]
verify_jwt = false

[functions.register-with-invite]
verify_jwt = false

[functions.set-user-password]
verify_jwt = false

[functions.send-password-reset]
verify_jwt = false

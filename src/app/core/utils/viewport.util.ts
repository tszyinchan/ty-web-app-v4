/** Shared narrow-viewport breakpoint for launcher density and similar layout. */
export const VIEWPORT_NARROW_MAX_PX = 768;

export const VIEWPORT_NARROW_MQ = `(max-width: ${VIEWPORT_NARROW_MAX_PX}px)`;
